# The end-to-end suite is a development tool; every dependency it has is a devDependency.
# Declaring `include=dev` stops a shell that exports NODE_ENV=production from producing an install
# in which Playwright is missing, which fails with a misleading "no tests found" instead of an
# install error.
include=dev

audit=false
fund=false
