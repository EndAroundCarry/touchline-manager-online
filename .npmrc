# The root package is a development task runner only, so its devDependencies are never optional.
# Declaring `include=dev` stops a shell that exports NODE_ENV=production from producing an install
# where `npm run dev` cannot find its tooling.
include=dev
audit=false
fund=false
